const {createAccount,forgetPassword,resetPassword,userLogin,userLogout} = require ('./userAuth')

module.exports = {
    createAccount,
    forgetPassword,
    resetPassword,
    userLogin,
    userLogout
  };